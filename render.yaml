services:
  - type: web
    name: infill-web
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: WORKER_SECRET
        sync: false
      - key: MODAL_ANALYZE_URL
        sync: false
